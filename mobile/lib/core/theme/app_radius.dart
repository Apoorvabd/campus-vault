/// Radius scale — use these instead of raw numbers in BorderRadius.
class AppRadius {
  AppRadius._();

  static const double pill = 999;
  static const double card = 16;
  static const double input = 12;
  static const double iconBox = 12;
}
